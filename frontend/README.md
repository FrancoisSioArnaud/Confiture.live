# Frontend Confiture

Application React + JavaScript démarrée avec Vite.

## Commandes

```bash
npm install
npm run dev
npm run test
npm run build
```

Le frontend reste volontairement sans TypeScript : uniquement des fichiers `.js` et `.jsx`.
