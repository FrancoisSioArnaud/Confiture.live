# Django Admin — V0

Le Django admin sert à inspecter les données persistées V0 :

- jams ;
- transactions ;
- events ;
- snapshots.

`JamClientSession` n'existe plus en V0 et ne doit pas être enregistré dans l'admin.

Les client sessions sont repoussées en V1.
